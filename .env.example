# Optional Codex authentication (the Codex CLI may use its own login state instead)
OPENAI_API_KEY=
# Optional default AWS region/profile. CLI flags take precedence.
AWS_REGION=us-east-1
AWS_PROFILE=
